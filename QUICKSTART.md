# Быстрый старт

## Первый запуск

1. **Установите зависимости:**
   ```bash
   npm install
   ```

2. **Добавьте иконки (опционально):**
   - Создайте папку `src-tauri/icons/`
   - Добавьте файлы: `32x32.png`, `128x128.png`, `128x128@2x.png`, `icon.icns`, `icon.ico`
   - Или временно закомментируйте секцию `icon` в `src-tauri/tauri.conf.json`

3. **Запустите приложение:**
   ```bash
   npm run tauri dev
   ```

## Использование WebP → PNG конвертера

1. Нажмите кнопку "📁 Выбрать папку"
2. Выберите папку с WebP файлами
3. Нажмите "🔄 Конвертировать"
4. Дождитесь завершения конвертации
5. После конвертации выберите:
   - **"Да, удалить"** - удалит исходные WebP файлы
   - **"Нет, оставить"** - оставит WebP файлы в папке

## Сборка для распространения

```bash
npm run tauri build
```

Собранное приложение будет в `src-tauri/target/release/bundle/msi/` (Windows installer).

## Настройка автообновления

См. файл `SETUP.md` для подробных инструкций по настройке автообновления через GitHub релизы.

